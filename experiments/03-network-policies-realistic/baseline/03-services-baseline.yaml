apiVersion: v1
kind: ConfigMap
metadata:
  name: netpol-baseline-config
  namespace: default
data:
  description: "Baseline configuration for Network Policies experiment."
---
apiVersion: v1
kind: Service
metadata:
  name: netpol-baseline
  namespace: default
spec:
  selector:
    app: netpol-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
